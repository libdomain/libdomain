services:
  testenv:
    build: testenv-container
    container_name: testenv
    restart: "no"
    environment:
      - LDAP_SERVER=ldap://directory-server:3890
    volumes:
      - data-volume:/app
    depends_on:
      directory:
        condition: service_started

volumes:
  data-volume:
    external: true

