services:
  testenv:
    build: testenv-container
    container_name: testenv
    restart: "no"
    volumes:
      - data-volume:/app
    depends_on:
      directory:
        condition: service_healthy

volumes:
  data-volume:
    external: true

